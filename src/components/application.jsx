import React, {Component} from 'react';
import {connect} from 'react-redux';

class ApplicationComponents extends Component {
  render() {
    return <div>

    </div>
  }
}

export const Application = connect()(ApplicationComponents);