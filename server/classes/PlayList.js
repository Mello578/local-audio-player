class PlayList{
  constructor(id, img, music, trackName, meta){
    this.id = id;
    this.img = img;
    this.music = music;
    this.trackName = trackName;
    this.meta = meta;
  }
}

module.exports = {PlayList};